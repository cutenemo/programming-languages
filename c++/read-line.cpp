//
//  main.cpp
//  uhunt
//
//  read line
//  2017/02/06
//

#include <iostream>

int main() {
    //freopen("sample.in", "r", stdin);

    int K = 0, E = 0, n = 0;
    while(scanf("%d%d\n", &K, &E) == 2) { // or != EOF both work
        n++;
    }

    std::string line;
    while (getline(std::cin, line)) {

    }

    char sentence[50];
    while (fgets(sentence, 50, stdin)) {

    }

    return 0;
}
